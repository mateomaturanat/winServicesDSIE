@model IEnumerable<Mechanic.Models.TypeDocument>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NameDocument)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescriptionDocument)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NameDocument)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescriptionDocument)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTypeDocument }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTypeDocument }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTypeDocument })
        </td>
    </tr>
}

</table>
