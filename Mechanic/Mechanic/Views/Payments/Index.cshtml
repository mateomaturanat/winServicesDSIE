@model IEnumerable<Mechanic.Models.Payment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ConfirmationCodePayment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescriptionPayment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ResponsePayment)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Service.MechanicCommentService)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ConfirmationCodePayment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescriptionPayment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ResponsePayment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Service.MechanicCommentService)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPayment }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPayment }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPayment })
        </td>
    </tr>
}

</table>
